const merge = require('./ll-merge');
const LinkedList = require('../linkedlist');

describe('MergeLL', () => {

    test('it can push a node in the list1', () => {
      expect(merge.value).toEqual();
    });
});
